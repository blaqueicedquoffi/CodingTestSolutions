package com.collection;

import javafx.geometry.Side;

public class RightSide {
    public static void main(String[] args) {

    }

   /* public Side LongerSide()
    {
        //end of link left hand side
        if(this.Left == null)
        {
            return Side.Right;
        }
        //end of link right hand side
        if(this.Right == null)
        {
            return Side.Left;
        }
        //if in a closed loop
        if(this.Right.Left == this.Left.Right)
        {
            return Side.None;
        }
        //Recursive call if in the middle of chain
        //Need to figure a way to propagate the stack
        return this.LongerSide();*/

}
